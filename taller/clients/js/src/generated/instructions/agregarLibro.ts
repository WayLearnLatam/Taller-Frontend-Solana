/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BIBLIOTECA_PROGRAM_ADDRESS } from "../programs";

export const AGREGAR_LIBRO_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  50, 223, 114, 112, 75, 75, 0, 104,
]);

export function getAgregarLibroDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    AGREGAR_LIBRO_DISCRIMINATOR,
  );
}

export type AgregarLibroInstruction<
  TProgram extends string = typeof BIBLIOTECA_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountLibro extends string | AccountMeta<string> = string,
  TAccountBiblioteca extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountLibro extends string
        ? WritableAccount<TAccountLibro>
        : TAccountLibro,
      TAccountBiblioteca extends string
        ? WritableAccount<TAccountBiblioteca>
        : TAccountBiblioteca,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AgregarLibroInstructionData = {
  discriminator: ReadonlyUint8Array;
  nombre: string;
  paginas: number;
};

export type AgregarLibroInstructionDataArgs = {
  nombre: string;
  paginas: number;
};

export function getAgregarLibroInstructionDataEncoder(): Encoder<AgregarLibroInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nombre", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["paginas", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: AGREGAR_LIBRO_DISCRIMINATOR }),
  );
}

export function getAgregarLibroInstructionDataDecoder(): Decoder<AgregarLibroInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nombre", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["paginas", getU16Decoder()],
  ]);
}

export function getAgregarLibroInstructionDataCodec(): Codec<
  AgregarLibroInstructionDataArgs,
  AgregarLibroInstructionData
> {
  return combineCodec(
    getAgregarLibroInstructionDataEncoder(),
    getAgregarLibroInstructionDataDecoder(),
  );
}

export type AgregarLibroInput<
  TAccountOwner extends string = string,
  TAccountLibro extends string = string,
  TAccountBiblioteca extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  libro: Address<TAccountLibro>;
  biblioteca: Address<TAccountBiblioteca>;
  systemProgram?: Address<TAccountSystemProgram>;
  nombre: AgregarLibroInstructionDataArgs["nombre"];
  paginas: AgregarLibroInstructionDataArgs["paginas"];
};

export function getAgregarLibroInstruction<
  TAccountOwner extends string,
  TAccountLibro extends string,
  TAccountBiblioteca extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BIBLIOTECA_PROGRAM_ADDRESS,
>(
  input: AgregarLibroInput<
    TAccountOwner,
    TAccountLibro,
    TAccountBiblioteca,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AgregarLibroInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountLibro,
  TAccountBiblioteca,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIBLIOTECA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    libro: { value: input.libro ?? null, isWritable: true },
    biblioteca: { value: input.biblioteca ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("libro", accounts.libro),
      getAccountMeta("biblioteca", accounts.biblioteca),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAgregarLibroInstructionDataEncoder().encode(
      args as AgregarLibroInstructionDataArgs,
    ),
    programAddress,
  } as AgregarLibroInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountLibro,
    TAccountBiblioteca,
    TAccountSystemProgram
  >);
}

export type ParsedAgregarLibroInstruction<
  TProgram extends string = typeof BIBLIOTECA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    libro: TAccountMetas[1];
    biblioteca: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AgregarLibroInstructionData;
};

export function parseAgregarLibroInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAgregarLibroInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      libro: getNextAccount(),
      biblioteca: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAgregarLibroInstructionDataDecoder().decode(instruction.data),
  };
}
