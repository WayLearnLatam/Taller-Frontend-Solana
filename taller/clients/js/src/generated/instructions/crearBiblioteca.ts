/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BIBLIOTECA_PROGRAM_ADDRESS } from "../programs";

export const CREAR_BIBLIOTECA_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([240, 50, 132, 205, 92, 245, 20, 198]);

export function getCrearBibliotecaDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREAR_BIBLIOTECA_DISCRIMINATOR,
  );
}

export type CrearBibliotecaInstruction<
  TProgram extends string = typeof BIBLIOTECA_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountBiblioteca extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountBiblioteca extends string
        ? WritableAccount<TAccountBiblioteca>
        : TAccountBiblioteca,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CrearBibliotecaInstructionData = {
  discriminator: ReadonlyUint8Array;
  nBiblioteca: string;
};

export type CrearBibliotecaInstructionDataArgs = { nBiblioteca: string };

export function getCrearBibliotecaInstructionDataEncoder(): Encoder<CrearBibliotecaInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nBiblioteca", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CREAR_BIBLIOTECA_DISCRIMINATOR }),
  );
}

export function getCrearBibliotecaInstructionDataDecoder(): Decoder<CrearBibliotecaInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nBiblioteca", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCrearBibliotecaInstructionDataCodec(): Codec<
  CrearBibliotecaInstructionDataArgs,
  CrearBibliotecaInstructionData
> {
  return combineCodec(
    getCrearBibliotecaInstructionDataEncoder(),
    getCrearBibliotecaInstructionDataDecoder(),
  );
}

export type CrearBibliotecaInput<
  TAccountOwner extends string = string,
  TAccountBiblioteca extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  biblioteca: Address<TAccountBiblioteca>;
  systemProgram?: Address<TAccountSystemProgram>;
  nBiblioteca: CrearBibliotecaInstructionDataArgs["nBiblioteca"];
};

export function getCrearBibliotecaInstruction<
  TAccountOwner extends string,
  TAccountBiblioteca extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BIBLIOTECA_PROGRAM_ADDRESS,
>(
  input: CrearBibliotecaInput<
    TAccountOwner,
    TAccountBiblioteca,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CrearBibliotecaInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountBiblioteca,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIBLIOTECA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    biblioteca: { value: input.biblioteca ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("biblioteca", accounts.biblioteca),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCrearBibliotecaInstructionDataEncoder().encode(
      args as CrearBibliotecaInstructionDataArgs,
    ),
    programAddress,
  } as CrearBibliotecaInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountBiblioteca,
    TAccountSystemProgram
  >);
}

export type ParsedCrearBibliotecaInstruction<
  TProgram extends string = typeof BIBLIOTECA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    biblioteca: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CrearBibliotecaInstructionData;
};

export function parseCrearBibliotecaInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCrearBibliotecaInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      biblioteca: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCrearBibliotecaInstructionDataDecoder().decode(instruction.data),
  };
}
