/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BIBLIOTECA_PROGRAM_ADDRESS } from "../programs";

export const ALTERNAR_ESTADO_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [202, 14, 23, 35, 47, 67, 27, 42],
);

export function getAlternarEstadoDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ALTERNAR_ESTADO_DISCRIMINATOR,
  );
}

export type AlternarEstadoInstruction<
  TProgram extends string = typeof BIBLIOTECA_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountLibro extends string | AccountMeta<string> = string,
  TAccountBiblioteca extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountLibro extends string
        ? WritableAccount<TAccountLibro>
        : TAccountLibro,
      TAccountBiblioteca extends string
        ? WritableAccount<TAccountBiblioteca>
        : TAccountBiblioteca,
      ...TRemainingAccounts,
    ]
  >;

export type AlternarEstadoInstructionData = {
  discriminator: ReadonlyUint8Array;
  nombre: string;
};

export type AlternarEstadoInstructionDataArgs = { nombre: string };

export function getAlternarEstadoInstructionDataEncoder(): Encoder<AlternarEstadoInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nombre", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: ALTERNAR_ESTADO_DISCRIMINATOR }),
  );
}

export function getAlternarEstadoInstructionDataDecoder(): Decoder<AlternarEstadoInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nombre", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getAlternarEstadoInstructionDataCodec(): Codec<
  AlternarEstadoInstructionDataArgs,
  AlternarEstadoInstructionData
> {
  return combineCodec(
    getAlternarEstadoInstructionDataEncoder(),
    getAlternarEstadoInstructionDataDecoder(),
  );
}

export type AlternarEstadoInput<
  TAccountOwner extends string = string,
  TAccountLibro extends string = string,
  TAccountBiblioteca extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  libro: Address<TAccountLibro>;
  biblioteca: Address<TAccountBiblioteca>;
  nombre: AlternarEstadoInstructionDataArgs["nombre"];
};

export function getAlternarEstadoInstruction<
  TAccountOwner extends string,
  TAccountLibro extends string,
  TAccountBiblioteca extends string,
  TProgramAddress extends Address = typeof BIBLIOTECA_PROGRAM_ADDRESS,
>(
  input: AlternarEstadoInput<TAccountOwner, TAccountLibro, TAccountBiblioteca>,
  config?: { programAddress?: TProgramAddress },
): AlternarEstadoInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountLibro,
  TAccountBiblioteca
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIBLIOTECA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    libro: { value: input.libro ?? null, isWritable: true },
    biblioteca: { value: input.biblioteca ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("libro", accounts.libro),
      getAccountMeta("biblioteca", accounts.biblioteca),
    ],
    data: getAlternarEstadoInstructionDataEncoder().encode(
      args as AlternarEstadoInstructionDataArgs,
    ),
    programAddress,
  } as AlternarEstadoInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountLibro,
    TAccountBiblioteca
  >);
}

export type ParsedAlternarEstadoInstruction<
  TProgram extends string = typeof BIBLIOTECA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    libro: TAccountMetas[1];
    biblioteca: TAccountMetas[2];
  };
  data: AlternarEstadoInstructionData;
};

export function parseAlternarEstadoInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAlternarEstadoInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      libro: getNextAccount(),
      biblioteca: getNextAccount(),
    },
    data: getAlternarEstadoInstructionDataDecoder().decode(instruction.data),
  };
}
