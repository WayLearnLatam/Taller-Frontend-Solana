/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { BIBLIOTECA_PROGRAM_ADDRESS } from "../programs";

export const ELIMINAR_LIBRO_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  131, 109, 56, 244, 173, 11, 206, 41,
]);

export function getEliminarLibroDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ELIMINAR_LIBRO_DISCRIMINATOR,
  );
}

export type EliminarLibroInstruction<
  TProgram extends string = typeof BIBLIOTECA_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountLibro extends string | AccountMeta<string> = string,
  TAccountBiblioteca extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountLibro extends string
        ? WritableAccount<TAccountLibro>
        : TAccountLibro,
      TAccountBiblioteca extends string
        ? WritableAccount<TAccountBiblioteca>
        : TAccountBiblioteca,
      ...TRemainingAccounts,
    ]
  >;

export type EliminarLibroInstructionData = {
  discriminator: ReadonlyUint8Array;
  nombre: string;
};

export type EliminarLibroInstructionDataArgs = { nombre: string };

export function getEliminarLibroInstructionDataEncoder(): Encoder<EliminarLibroInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nombre", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: ELIMINAR_LIBRO_DISCRIMINATOR }),
  );
}

export function getEliminarLibroInstructionDataDecoder(): Decoder<EliminarLibroInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nombre", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getEliminarLibroInstructionDataCodec(): Codec<
  EliminarLibroInstructionDataArgs,
  EliminarLibroInstructionData
> {
  return combineCodec(
    getEliminarLibroInstructionDataEncoder(),
    getEliminarLibroInstructionDataDecoder(),
  );
}

export type EliminarLibroInput<
  TAccountOwner extends string = string,
  TAccountLibro extends string = string,
  TAccountBiblioteca extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  libro: Address<TAccountLibro>;
  biblioteca: Address<TAccountBiblioteca>;
  nombre: EliminarLibroInstructionDataArgs["nombre"];
};

export function getEliminarLibroInstruction<
  TAccountOwner extends string,
  TAccountLibro extends string,
  TAccountBiblioteca extends string,
  TProgramAddress extends Address = typeof BIBLIOTECA_PROGRAM_ADDRESS,
>(
  input: EliminarLibroInput<TAccountOwner, TAccountLibro, TAccountBiblioteca>,
  config?: { programAddress?: TProgramAddress },
): EliminarLibroInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountLibro,
  TAccountBiblioteca
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIBLIOTECA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    libro: { value: input.libro ?? null, isWritable: true },
    biblioteca: { value: input.biblioteca ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("libro", accounts.libro),
      getAccountMeta("biblioteca", accounts.biblioteca),
    ],
    data: getEliminarLibroInstructionDataEncoder().encode(
      args as EliminarLibroInstructionDataArgs,
    ),
    programAddress,
  } as EliminarLibroInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountLibro,
    TAccountBiblioteca
  >);
}

export type ParsedEliminarLibroInstruction<
  TProgram extends string = typeof BIBLIOTECA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    libro: TAccountMetas[1];
    biblioteca: TAccountMetas[2];
  };
  data: EliminarLibroInstructionData;
};

export function parseEliminarLibroInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedEliminarLibroInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      libro: getNextAccount(),
      biblioteca: getNextAccount(),
    },
    data: getEliminarLibroInstructionDataDecoder().decode(instruction.data),
  };
}
